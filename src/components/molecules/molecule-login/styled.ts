import styled from 'styled-components';

const ContentImage = styled.div`
	padding: 5%;
	text-align: center;
`;

const Image = styled.img`
	border-radius: 50%;
	height: 120px;
	width: 120px;
`;

const ContentIcons = styled.div`
	display: flex;
	justify-content: center;
`;

export { ContentImage, Image, ContentIcons };
